# DevOps Cloud + Kubernetes Flask Project

This repo is an end-to-end DevOps learning project.

## App
- Flask API + simple UI
- Endpoints:
  - GET /health
  - GET /api/hello
  - GET /

## Goal
Build a complete pipeline:
Git → Build → Test → Docker → Registry → Terraform → EKS → Helm → Monitoring → Security

